const add = (x, y) => {
    return x+ y;
}

/**
 * @description 删除
 * @returns bool
 */
const del = (x, y) => {
    return delete x[y];;
}

// desc-use-for
const remove = () => {
    return true;
}

const addLeft = () => {
    return 'left';
}